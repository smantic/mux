name: go-get-test
on: 
  push:
    tags:
      - '*'
jobs: 
  build: 
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Go
      uses: actions/setup-go@v3
    - run: echo "RELEASE_VERSION=${GITHUB_REF#refs/*/}" >> $GITHUB_ENV
    - run: |  
        go version 
        go get go.smantic.dev/mux@${{ env.RELEASE_VERSION }}
